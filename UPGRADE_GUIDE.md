# อัปเดตเป็นรุ่นบอร์ดเดียว

1. สำรอง smart_air_db จาก HeidiSQL และเก็บค่าตั้งเดิมไว้ หยุด Node ก่อนดึงโค้ด
2. ใช้ Node.js 22 ขึ้นไป เปิด Laragon แล้วดับเบิลคลิก Start-AirWatch.cmd
3. ครั้งแรกตัวช่วยถามข้อมูล DB และสร้าง `.env` ให้ ใช้ JWT_SECRET เดิมจาก environment ถ้ามี ไม่เขียนทับ admin ที่สร้างไว้แล้ว
4. ถ้า DB มีบอร์ดเดียว ใช้บอร์ด/key เดิม ถ้าไม่มีจะสร้างอัตโนมัติ หากมีหลายบอร์ดตัวช่วยให้เลือกหนึ่งตัวโดยไม่ลบตัวอื่น
5. หน้าเว็บไม่มีปุ่มเพิ่มบอร์ดหรือตัวเลือกหลายบอร์ดแล้ว เข้าด้วย admin เดิมและเปิดรหัสตั้งค่าจากหน้าผู้ดูแล
6. อัปโหลด firmware ใหม่ครั้งหนึ่งพร้อม header ทั้งสาม (`sensor_filter.h`, `connection_config.h`, `tls_roots.h`) โดยไม่ต้องเปลี่ยน IP หรือ Wi-Fi ในโค้ด
7. เชื่อม AirWatch-Setup → เลือก Wi-Fi → วาง Connection code จากเว็บ → บันทึก
8. ครั้งถัดไปบอร์ดจำค่าเอง; เปลี่ยน Wi-Fi โดยกด BOOT 3 วินาทีแล้วปล่อยขณะบอร์ดทำงาน

บอร์ด firmware เก่ายังส่งได้หากใช้ key ส่วนตัวเดิม แต่จะยังมีขั้นตอนตั้งค่าแบบเก่าจนกว่าจะอัปโหลดใหม่ รหัส demo ที่เคยเผยแพร่ถูกปฏิเสธ ให้เปลี่ยนผ่านเว็บ

ถ้า server เคยมี `.env` อยู่แล้ว ตัวช่วยไม่เขียนทับไฟล์นั้น เพื่อรักษา secret/บัญชีเดิม กรณีค่าตั้งไม่ตรงหรือเลือกหลายบอร์ดต้องให้ผู้ดูแลปรับค่าตั้งการติดตั้ง (ไม่แก้ source) อย่าลบไฟล์เดิมโดยไม่สำรอง

## ตรวจรับกับของจริง

- [ ] เปิดทุกหน้าและสลับธีมบนคอม/มือถือ หน้าตาเดิมยังอยู่ ไม่มีเมนูเพิ่ม/เลือกอุปกรณ์
- [ ] ลงชื่อเข้าใช้ admin เดิม เห็นบอร์ดและประวัติของบอร์ดเดิมทันที
- [ ] บัญชีที่ไม่ใช่เจ้าของ/admin อ่านค่าหรือรับ Socket.IO ของบอร์ดไม่ได้
- [ ] คัดลอกรหัสตั้งค่าแล้วนำไปใส่ ESP32 ได้โดยไม่แก้โค้ด
- [ ] รีบูตบอร์ด/server แล้วกลับมาเชื่อมได้โดยไม่ตั้งซ้ำ
- [ ] ใช้โดเมนจริงผ่าน HTTPS ส่งข้อมูลสำเร็จ; ใบรับรองไม่ถูกต้องต้องเชื่อมไม่ผ่าน
- [ ] ในโหมด HTTPS เมื่อ DNS/TLS ล้มเหลวต้องไม่เปลี่ยนเป็น HTTP หรือ LAN discovery
- [ ] เปลี่ยน key แล้ว key เก่าไม่รับข้อมูล ตั้งรหัสใหม่ผ่าน BOOT/portal ได้
- [ ] เครือข่าย LAN เปลี่ยน IP server แล้ว AUTO ค้นหาใหม่ได้
- [ ] ถอด PMS/หยุดบอร์ด → ไม่มีข้อมูล/ออฟไลน์ ไม่แสดงพัดลม OFF ทั้งที่ไม่ทราบสถานะ
- [ ] PM 34 ค่อยเพิ่มเป็น 37 → ตัวกรองข้าม threshold ได้; รีเลย์ทำงานตรงโหมด
- [ ] ตรวจ latency การควบคุมขณะ Wi-Fi/TLS ล้มเหลว เพราะ HTTPClient เป็น synchronous
- [ ] migration รันซ้ำได้ ข้อมูล FLOAT/DECIMAL ไม่ถูกตัดทศนิยม

ข้อมูลเดิมที่ device_id เป็น NULL ไม่ถูกเดาเจ้าของ และไม่เปิดผ่านเว็บอัตโนมัติ อย่าผูกประวัติทั้งหมดกับบอร์ดโดยไม่ตรวจที่มา

Cloud setup: ดู DEPLOYMENT.md ต้องมี server และ domain จริงก่อน deploy; รายการนี้ยังไม่ถือว่าผ่านจนกว่าจะได้ลองกับอุปกรณ์และเครือข่ายจริง
