# อัปเดต AirWatch และตรวจรับบน Laragon

## อัปเดตซอฟต์แวร์

1. สำรอง smart_air_db เป็น SQL ผ่าน HeidiSQL และเก็บค่า environment เดิมไว้
2. หยุด Node server ก่อนอัปเดต ตรวจ `git status` แล้วดึง branch/commit ที่ต้องการทดสอบ ห้ามใช้ reset --hard หากมีงานที่ยังไม่บันทึก
3. รัน `npm ci` แล้วตั้ง DB_HOST, DB_PORT, DB_USER, DB_PASSWORD, DB_NAME ให้ตรง Laragon
4. รัน `npm run migrate` ต้องจบด้วย Migration completed หากไม่ผ่านให้แก้ error ก่อนทำต่อ คำสั่งนี้รันซ้ำได้และไม่เปลี่ยน FLOAT เป็น INT
5. สร้างและเก็บ JWT_SECRET ถาวรอย่างน้อย 32 ตัวอักษร หากมีบัญชี demo เดิม ให้เปลี่ยนรหัสด้วย `npm run seed -- --reset-password` ตาม README และเปลี่ยน JWT_SECRET เพื่อเพิกถอน session เดิม
6. ใช้ `npm run seed` เฉพาะเมื่อต้องสร้างผู้ดูแลใหม่ การสมัครหน้าเว็บไม่ให้สิทธิ์ admin
7. รัน `npm test` แล้ว `npm start` เปิดเว็บด้วย URL ของ Node เช่น http://localhost:3000 ไม่ใช่เปิดไฟล์ HTML โดยตรง
8. เข้าสู่ระบบใหม่ สร้าง/เลือกอุปกรณ์ คัดลอก key ของอุปกรณ์นั้นไปตั้งบอร์ด รหัส demo เดิมใช้ไม่ได้
9. อัปโหลด firmware พร้อม sensor_filter.h ในโฟลเดอร์เดียวกัน หากเปลี่ยน key ที่เก็บไว้บนบอร์ดให้ล้าง NVS/Flash แล้วตั้งใหม่

## ตรวจฐานข้อมูล

เปิด Query ใน HeidiSQL เลือก smart_air_db แล้วรัน:

```sql
SHOW COLUMNS FROM sensor_data;
SHOW INDEX FROM sensor_data;
SELECT id, device_id, in_pm25, temperature, created_at
FROM sensor_data ORDER BY id DESC LIMIT 10;
```

- มี device_id และ sensor columns อนุญาต NULL; ทศนิยมในประวัติเดิมยังอยู่
- การส่งข้อมูลใหม่มี device_id ตรงกับอุปกรณ์ที่เลือก
- ประวัติเดิมที่ device_id เป็น NULL ถูกเก็บไว้แต่ไม่เปิดให้บัญชีใดเห็นอัตโนมัติ ต้องตรวจที่มาให้ชัดก่อนผูก ห้าม UPDATE ประวัติทั้งหมดไปยังอุปกรณ์หนึ่งโดยเดา
- ตาราง users/devices มีอยู่ และ migrate รอบที่สองสำเร็จ

## ตรวจการทำงานครบวงจร

- [ ] เปิด Login, Dashboard, History และ Admin บนคอมและมือถือ ไม่มีข้อความหรือปุ่มล้นจอ ธีมสว่าง/มืดทำงานตรงกัน
- [ ] สมาชิก A ดูข้อมูลและ key ของ B ไม่ได้ แม้แก้ device_id ใน URL; สมาชิกเข้า Admin ไม่ได้
- [ ] เปิด A/B สองเบราว์เซอร์ ส่งข้อมูลบอร์ด A แล้ว B ไม่ได้รับข้อมูลของ A; ผู้ดูแลดูได้ทั้งสอง
- [ ] สร้างอุปกรณ์และกรอก key ในบอร์ด → telemetry ถูกบันทึก และกราฟแสดงในอุปกรณ์ที่เลือก
- [ ] เปลี่ยน key → key เก่ารับ 401, key ใหม่ส่งสำเร็จ (ลบ/ปรับ DEVICE_API_KEY เดิมใน environment ด้วย)
- [ ] สลับอุปกรณ์เร็ว ๆ แล้วค่าล่าสุด/กราฟไม่ปะปน ลองอุปกรณ์ไม่มีประวัติด้วย
- [ ] หยุดบอร์ด 15 วินาที → แสดงออฟไลน์และพัดลม “ไม่ทราบ”; กลับมาเชื่อมต่อแล้วข้อมูลกลับมา
- [ ] ถอด PMS → ค่าเป็นไม่มีข้อมูลภายใน timeout; ต่อกลับ → ค่าเริ่มจากตัวอย่างใหม่ ไม่ติดค่าตัวกรองเก่า
- [ ] PM ค่อย ๆ เพิ่มจาก 34 ไป 37 → ตัวกรองเพิ่มผ่าน 35 ได้; ตรวจรีเลย์ตามโหมดและเวลาหน่วงจริง
- [ ] ค่า 0 แสดง 0; NULL แสดงไม่มีข้อมูล ไม่ถูกวาดเป็น 0 ในกราฟ
- [ ] หน้า History โหลดและส่งออก CSV ได้ เปิดไฟล์แล้ววันที่/ค่าตรงตาราง
- [ ] หยุด MySQL → ไม่รายงานบันทึกสำเร็จและไม่เผยรายละเอียด SQL ใน response
- [ ] รีบูต server ด้วย JWT_SECRET เดิมและทดสอบเชื่อมต่อใหม่
- [ ] ทดสอบ captive portal เกิน 30 วินาที, การเปลี่ยน IP, ข้อมูลเซนเซอร์ขาด และ latency ขณะ Wi-Fi ล่มบนบอร์ดจริง

## เพิ่มเซนเซอร์นอกบ้านภายหลัง

รุ่นนี้ยังใช้ ESP32 ตัวเดียวและการต่อสาย เลือก `HAS_OUTDOOR_SENSORS true` เมื่อมี PMS นอกบ้าน RX4/TX5, MQ-2 GPIO35 และพัดลมกรองผ่านรีเลย์ GPIO26 ครบ ตรวจตารางโหมดควบคุมใน README และทดสอบกรณีเซนเซอร์นอกบ้านขาดก่อนใช้

ESP-NOW, MQTT, OTA, QR binding, Push notifications และการติดตามอายุแผ่นกรองยังเป็นงานในอนาคต ไม่รวมในรุ่นนี้

## ขอบเขตการตรวจรับ

Automated tests ตรวจตรรกะ server/UI, Socket.IO, migration และ C++ filter; งานนี้ยังต้องตรวจ firmware ทั้ง sketch กับ board/library รุ่นที่ใช้จริง รวมถึงหน้าจอใน browser และฐานข้อมูล Laragon ของผู้ใช้

HTTP/UDP ใน firmware เหมาะกับ LAN ที่เชื่อถือได้ การเปิดสู่ Internet ต้องทำ HTTPS และการตั้งค่า firmware/server สำหรับปลายทางจริงเพิ่มเติม
